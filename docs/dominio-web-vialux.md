# Dominio y presencia web de VIALUX — hallazgos 18 ago 2026

**Estado actual: VIALUX no tiene sitio web.** Investigación de dominios hecha el 18-ago-2026 (sesión en ~/PERSONAL):

- `vialux.com.mx` — **NO es nuestro**: WordPress placeholder de un tercero ("Sólo otro sitio de WordPress").
- `vialux.mx` — **tomado desde 2009** (titular particular en San Pedro Garza García; registro vigente hasta 2029). No disponible.
- `vialuxmx.com` — de un proveedor de internet ("VIALUX Internet", app en Google Play). No es nuestro.
- Colisión de marca a considerar: existe ViALUX GmbH (Alemania, óptica industrial) además del ISP mexicano.

**Decisión pendiente:** elegir dominio variante. Ideas a verificar disponibilidad cuando se decida: `vialuxsenalizacion.mx`, `grupovialux.mx`, `vialuxvial.mx`, o similar.

**Al construir el sitio, desde el día 1** (lección aprendida con latticeworks.mx, que nació invisible para Google):
- Usar la skill `vialux-brand-system` para lo visual.
- FAQ con respuestas directas en la primera línea + datos concretos visibles.
- Schema JSON-LD (LocalBusiness + FAQPage) y `llms.txt` en la raíz.
- NAP (nombre-dirección-teléfono) idéntico en sitio y directorios.
- Alta inmediata en Google Search Console (enviar sitemap) y Google Business Profile.

Referencia ampliada: `~/PERSONAL/docs/prompts-ai-rescatados.md` (sección 8, checklist GEO).
